```python\nimport numpy as np\nimport matplotlib.pyplot as plt\n\ndef seir_model(beta, gamma, sigma, N, E0, I0, R0, tmax, dt):\n    num_steps = int(tmax / dt)\n    t = np.linspace(0, tmax, num=num_steps)\n\n    S = np.zeros(num_steps)\n    E = np.zeros(num_steps)\n    I = np.zeros(num_steps)\n    R = np.zeros(num_steps)\n\n    S[0] = N - (E0 + I0 + R0)\n    E[0] = E0\n    I[0] = I0\n    R[0] = R0\n\n    for i in range(1, num_steps):\n        dS_dt = -beta * S[i-1] * I[i-1] / N\n        dE_dt = beta * S[i-1] * I[i-1] / N - sigma * E[i-1]\n        dI_dt = sigma * E[i-1] - gamma * I[i-1]\n        dR_dt = gamma * I[i-1]\n\n        S_star = S[i-1] + dt * dS_dt\n        E_star = E[i-1] + dt * dE_dt\n        I_star = I[i-1] + dt * dI_dt\n        R_star = R[i-1] + dt * dR_dt\n\n        dS_star_dt = -beta * S_star * I_star / N\n        dE_star_dt = beta * S_star * I_star / N - sigma * E_star\n        dI_star_dt = sigma * E_star - gamma * I_star\n        dR_star_dt = gamma * I_star\n\n        S[i] = S[i-1] + (dt / 4) * (dS_dt + 3 * dS_star_dt)\n        E[i] = E[i-1] + (dt / 4) * (dE_dt + 3 * dE_star_dt)\n        I[i] = I[i-1] + (dt / 4) * (dI_dt + 3 * dI_star_dt)\n        R[i] = R[i-1] + (dt / 4) * (dR_dt + 3 * dR_star_dt)\n\n    return t, S, E, I, R\n\n# Parameters\nbeta = 0.2\ngamma = 0.1\nsigma = 0.5\nN = 1000\nE0 = 1\nI0 = 1\nR0 = 0\ntmax = 20\ndt = 0.1\n\nt, S, E, I, R = seir_model(beta, gamma, sigma, N, E0, I0, R0, tmax, dt)\n\nplt.plot(t, S, label="Susceptible")\nplt.plot(t, E, label="Exposed")\nplt.plot(t, I, label="Infected")\nplt.plot(t, R, label="Recovered")\nplt.xlabel("Time")\nplt.ylabel("Population")\nplt.title("SEIR Model")\nplt.legend()\nplt.show()\n```
